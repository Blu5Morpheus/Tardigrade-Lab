# Force CPU-only PyTorch wheel before sentence-transformers pulls it in.
# This keeps the install ~200 MB lighter than the GPU build, which matters
# on Render free-tier disk + memory.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

streamlit==1.40.1
streamlit-authenticator==0.4.1

# NB: numpy must be <2.1 because pennylane 0.39 caps it there.
numpy==2.0.2
scipy==1.14.1
matplotlib==3.9.2
plotly==5.24.1
pandas==2.2.3

pennylane==0.39.0
pennylane-lightning==0.39.0
# autoray 0.7+ renamed NumpyMimic; pennylane 0.39 still imports the old symbol,
# so any deploy that resolves a fresh autoray crashes with AttributeError on
# train. Pin to the last 0.6.x release until we bump pennylane to 0.40+.
autoray==0.6.12

supabase==2.10.0
PyGithub==2.5.0
python-dotenv==1.0.1
requests==2.32.3
PyYAML==6.0.2
bcrypt==4.2.1
psutil==6.1.0

# me-bot
sentence-transformers==3.3.1
groq==0.13.0
